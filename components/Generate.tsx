import React, { useState } from 'react';
import { css, jsx } from '@emotion/react';
import { Mediaquery } from '../util/Style.config';

export default function Generate({}) {
  return (
    <div className="relative bg-white overflow-hidden" id="generate">
      <p>aaa</p>
    </div>

  );
}
